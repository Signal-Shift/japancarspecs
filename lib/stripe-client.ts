/**
 * Stripe.js — use when Checkout Session URLs are returned from your API.
 * Example: const stripe = await loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY!)
 */
export { loadStripe } from "@stripe/stripe-js"
